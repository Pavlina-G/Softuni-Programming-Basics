from math import pi
number_r = float(input())
radius = number_r
calculated_area = pi * radius * radius
calculated_parameter = pi * radius * 2
print ('%.2f' %(calculated_area))
print ('%.2f' %(calculated_parameter))

